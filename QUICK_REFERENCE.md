# دليل سريع - خوارزمية حساب الخرسانة
# Quick Reference Guide - Concrete Calculation Algorithm

## 🚀 البدء السريع (Quick Start)

### الخطوة 1: تشغيل الخادم
```bash
cd backend
npm install
npm start
# أو
node server.js
```

### الخطوة 2: تشغيل الواجهة الأمامية
```bash
cd frontend
npm install
npm run dev
```

### الخطوة 3: الوصول إلى الأداة
```
http://localhost:3000/concrete-calculator
```

---

## 📝 صيغة الطلب (Request Format)

```http
POST /api/calculations/concrete-algorithm
Content-Type: application/json

{
  "cleaningPourLength": 20.5,        // متر
  "cleaningPourWidth": 15.3,          // متر
  "cleaningPourHeight": 0.10,         // متر
  "numberOfFloors": 5,                // عدد الطوابق
  "slabArea": 300,                    // متر مربع
  "soilType": "ObjectId",             // معرف نوع التربة
  "buildingType": "سكني",             // نوع المبنى
  "foundationHeight": 0.50,           // متر
  "numberOfFoundations": 4,           // عدد القواعس
  "foundationShape": "square",        // مربعة أو rectangle
  "areFoundationsSimilar": true,      // متشابهة؟
  "foundationDetails": [],            // للقواعس المختلفة
  "projectId": "ObjectId"             // (اختياري) معرف المشروع
}
```

---

## 📊 صيغة الاستجابة (Response Format)

```json
{
  "success": true,
  "inputs": {
    "cleaningPourLength": 20.5,
    "cleaningPourWidth": 15.3,
    "cleaningPourHeight": 0.10,
    "numberOfFloors": 5,
    "slabArea": 300,
    "buildingType": "سكني",
    "foundationHeight": 0.50,
    "numberOfFoundations": 4,
    "foundationShape": "square",
    "areFoundationsSimilar": true
  },
  "calculations": {
    "cleaningPourVolume": 31.365,
    "loads": {
      "deadLoad": 3.5,
      "liveLoad": 1.5,
      "totalLoad": 5.0
    },
    "soil": {
      "name": "رمل",
      "bearingCapacity": 0.3,
      "bearingCapacityMin": 0.2,
      "bearingCapacityMax": 0.4
    },
    "building": {
      "totalLoad": 7500,
      "foundationArea": 25,
      "foundationDimensions": {
        "length": 5,
        "width": 5,
        "actualArea": 25
      }
    },
    "foundations": {
      "totalVolume": 613.15,
      "breakdown": [
        {
          "index": 1,
          "volume": 153.23,
          "count": 4,
          "height": 0.50
        }
      ]
    }
  },
  "summary": {
    "cleaningPourVolume": 31.365,
    "foundationArea": 25,
    "foundationDimensions": "5 × 5",
    "foundationsVolume": 613.15
  }
}
```

---

## 🔢 الصيغ الرياضية (Mathematical Formulas)

### 1. حجم صبة النظاف
```
V_cleaning = Length × Width × Height
```

### 2. مساحة القاعدة
```
B_area = (P × n × (D + L)) / σ_soil

حيث:
P = مساحة البلاطة
n = عدد الطوابق
D = الحمل الميت
L = الحمل الحي
σ_soil = قدرة التربة
```

### 3. أبعاد القاعدة المستطيلة
```
W = √(B_area ÷ 1.2)
L = W × 1.2
```

### 4. أبعاد القاعدة المربعة
```
L = W = √B_area
```

### 5. حجم القواعس
```
V_foundations = (L_eff × W_eff × H) × n_foundations

حيث:
L_eff = L_cleaning - 0.2
W_eff = W_cleaning - 0.2
H = ارتفاع القاعدة
```

---

## ⚙️ البيانات المطلوبة (Required Data)

### من المستخدم مباشرة:
- ✓ طول صبة النظاف (م)
- ✓ عرض صبة النظاف (م)
- ✓ ارتفاع صبة النظاف (م)
- ✓ عدد الطوابق
- ✓ مساحة البلاطة (م²)
- ✓ نوع التربة (من قاعدة البيانات)
- ✓ نوع المبنى
- ✓ ارتفاع القاعدة (م)
- ✓ عدد القواعس
- ✓ شكل القاعدة
- ✓ هل القواعس متشابهة؟

### من قاعدة البيانات تلقائياً:
- 📊 الحمل الحي (LiveLoad)
- 📊 الحمل الميت (DeadLoad)
- 📊 قدرة تحمل التربة (SoilType)

---

## ✅ قواعد التحقق (Validation Rules)

| الحقل | القاعدة | الخطأ |
|------|--------|------|
| طول صبة النظاف | > 0 | طول صبة النظاف يجب أن يكون أكبر من صفر |
| عرض صبة النظاف | > 0 | عرض صبة النظاف يجب أن يكون أكبر من صفر |
| ارتفاع صبة النظاف | > 0 | ارتفاع صبة النظاف يجب أن يكون أكبر من صفر |
| عدد الطوابق | > 0 | عدد الطوابق يجب أن يكون أكبر من صفر |
| مساحة البلاطة | > 0 | مساحة البلاطة يجب أن تكون أكبر من صفر |
| ارتفاع القاعدة | 0.40-0.80 | ارتفاع القاعدة يجب أن يكون بين 0.40 و 0.80 متر |
| عدد القواعس | > 0 | عدد القواعس يجب أن يكون أكبر من صفر |
| شكل القاعدة | square/rectangle | شكل القاعدة غير صالح |

---

## 🎯 الحالات الاستثنائية (Edge Cases)

### حالة 1: قواعس مختلفة
```javascript
foundationDetails: [
  { index: 1, height: 0.50 },
  { index: 2, height: 0.60 },
  { index: 3, height: 0.55 }
]
// يتم حساب كل قاعدة على حدة ثم جمع النتائج
```

### حالة 2: نسبة الطول للعرض
```javascript
// للقاعدة المستطيلة يتم استخدام نسبة ثابتة
length = width × 1.2  // النسبة = 1.2
```

### حالة 3: هامش الأمان
```javascript
// عند حساب القواعس يتم طرح 0.20 م من كل جانب
effectiveLength = cleaningPourLength - 0.20
effectiveWidth = cleaningPourWidth - 0.20
```

---

## 🔍 أمثلة سريعة (Quick Examples)

### مثال 1: مبنى سكني صغير
```
المدخلات:
- صبة النظاف: 15 × 12 × 0.10 م
- 3 طوابق، 180 م²
- تربة رملية
- 4 قواعس مربعة بارتفاع 0.50 م

النتائج:
- صبة النظاف: 18 م³
- القواعس: ~430 م³
- الإجمالي: ~448 م³
```

### مثال 2: مبنى تجاري كبير
```
المدخلات:
- صبة النظاف: 40 × 30 × 0.15 م
- 10 طوابق، 1200 م²
- تربة حصى
- 8 قواعس مستطيلة بارتفاع 0.70 م

النتائج:
- صبة النظاف: 180 م³
- القواعس: ~4000 م³
- الإجمالي: ~4180 م³
```

---

## 🛠️ استكشاف الأخطاء (Troubleshooting)

### المشكلة: "نوع التربة غير موجود"
**الحل**: تأكد من وجود نوع التربة في قاعدة البيانات
```bash
# تشغيل السكريبت الخاص بـ seeding
npm run seed:engineering
```

### المشكلة: "بيانات الحمل غير موجودة"
**الحل**: تأكد من إدارة البيانات الهندسية
```bash
npm run seed:engineering-data
```

### المشكلة: "ارتفاع القاعدة خارج النطاق"
**الحل**: استخدم قيمة بين 0.40 و 0.80 متر
```javascript
// ✓ صحيح
foundationHeight: 0.50

// ✗ خطأ
foundationHeight: 0.30  // أقل من 0.40
foundationHeight: 0.90  // أكبر من 0.80
```

---

## 📁 الملفات الرئيسية (Key Files)

| الملف | الوصف |
|------|--------|
| `backend/services/concreteCalculationService.js` | خدمة الحساب |
| `backend/routes/calculations.js` | API endpoints |
| `frontend/src/components/forms/ConcreteAlgorithmForm.tsx` | الواجهة |
| `CONCRETE_ALGORITHM_DOCUMENTATION.md` | توثيق شامل |
| `ALGORITHM_EXAMPLES_AND_TESTS.js` | أمثلة واختبارات |

---

## 🔗 الروابط المهمة (Important Links)

| الرابط | الوصف |
|-------|--------|
| `/api/calculations/concrete-algorithm` | API للحساب |
| `/api/calculations/project/:projectId` | جلب الحسابات |
| `POST /api/calculations/concrete-algorithm` | حساب جديد |

---

## 💡 نصائح مهمة (Tips)

1. **استخدم القيم الشائعة**: إذا كانت هناك قيمة شائعة للحمل، سيتم استخدامها تلقائياً
2. **بيانات دقيقة**: استخدم بيانات دقيقة للحصول على نتائج موثوقة
3. **التحقق من النطاق**: تأكد من أن ارتفاع القاعدة بين 0.40 و 0.80 متر
4. **حفظ النتائج**: احفظ النتائج في المشروع للعودة إليها لاحقاً

---

## 📞 الدعم (Support)

للمساعدة أو الأسئلة:
1. راجع `CONCRETE_ALGORITHM_DOCUMENTATION.md` للتفاصيل الشاملة
2. راجع `ALGORITHM_EXAMPLES_AND_TESTS.js` للأمثلة العملية
3. تحقق من رسائل الخطأ للمزيد من المعلومات

---

**آخر تحديث**: 14 ديسمبر 2025  
**الإصدار**: 1.0
