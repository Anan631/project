# تعليمات إدخال البيانات الهندسية

## المشكلة
إذا ظهرت رسالة خطأ "بيانات الحمولات غير موجودة لهذا النوع من المباني"، فهذا يعني أن البيانات الهندسية لم يتم إدخالها في قاعدة البيانات بعد.

## الحل

### 1. تأكد من أن MongoDB يعمل
```bash
# تحقق من أن MongoDB يعمل على جهازك
```

### 2. شغل seeding script
```bash
cd backend
npm run seed:engineering
```

أو مباشرة:
```bash
cd backend
node scripts/seed-engineering-data.js
```

### 3. تحقق من النجاح
يجب أن ترى رسائل مثل:
```
✅ Connected to MongoDB
🗑️  Cleared existing data
✅ Inserted 7 soil types
✅ Inserted 10 live loads
✅ Inserted 25 dead loads
✅ Inserted 10 iron bars
✅ Inserted 3 roof types

🎉 All data seeded successfully!
```

## أنواع المباني المدعومة

### LiveLoad (الحمولات الحية):
1. المباني السكنية
2. المكاتب
3. المباني التجارية
4. المباني الصناعية
5. المستودعات والتخزين
6. المسارح والأماكن العامة وقاعات الاجتماعات
7. المدارس
8. المباني الرياضية (مثل الملاعب)
9. المستشفيات
10. مواقف السيارات

### DeadLoad (الحمولات الميتة):
جميع أنواع المباني المذكورة أعلاه لها بيانات DeadLoad.

## ملاحظات
- إذا قمت بتشغيل seeding script مرة أخرى، سيتم حذف البيانات القديمة وإدخال البيانات الجديدة.
- تأكد من أن ملف `.env` يحتوي على `MONGO_URI` الصحيح.

